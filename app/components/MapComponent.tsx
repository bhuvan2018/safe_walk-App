"use client"

import { forwardRef } from "react"
import "leaflet/dist/leaflet.css"

// ... rest of your existing Map component code ...
// Copy the content from your current Map.tsx

export default forwardRef(function MapComponent(props, ref) {
  // Your existing Map component code
})

